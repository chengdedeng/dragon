<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
	http://code.alibabatech.com/schema/dubbo
	http://code.alibabatech.com/schema/dubbo/dubbo.xsd http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit.xsd http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd"
       default-autowire="byType">
    <aop:aspectj-autoproxy proxy-target-class="true"/>
    <context:component-scan base-package="info.yangguo"/>
    <import resource="classpath*:spring/*.xml"/>

    <dubbo:service interface="info.yangguo.dragon.common.service.TraceService"
                   ref="traceServiceImpl"/>

    <!--走消息队列-->
    <rabbit:connection-factory id="rabbitConnectionFactory"
                               addresses="${rabbitmq.address}"
                               username="${rabbitmq.username}"
                               password="${rabbitmq.password}"/>

    <rabbit:template id="rabbitTemplate" connection-factory="rabbitConnectionFactory"
                     exchange="LogExchange"
                     routing-key="trace"/>

    <rabbit:admin connection-factory="rabbitConnectionFactory"/>
    <rabbit:queue name="traceQueue"/>
    <rabbit:topic-exchange name="LogExchange">
        <rabbit:bindings>
            <rabbit:binding queue="traceQueue" pattern="trace"/>
        </rabbit:bindings>
    </rabbit:topic-exchange>
    <!--根据消费情况适当添加消费者-->
    <rabbit:listener-container connection-factory="rabbitConnectionFactory">
        <rabbit:listener ref="producer" method="handleMessage" queue-names="traceQueue"/>
    </rabbit:listener-container>
    <rabbit:listener-container connection-factory="rabbitConnectionFactory">
        <rabbit:listener ref="producer" method="handleMessage" queue-names="traceQueue"/>
    </rabbit:listener-container>
    <rabbit:listener-container connection-factory="rabbitConnectionFactory">
        <rabbit:listener ref="producer" method="handleMessage" queue-names="traceQueue"/>
    </rabbit:listener-container>
    <rabbit:listener-container connection-factory="rabbitConnectionFactory">
        <rabbit:listener ref="producer" method="handleMessage" queue-names="traceQueue"/>
    </rabbit:listener-container>
    <rabbit:listener-container connection-factory="rabbitConnectionFactory">
        <rabbit:listener ref="producer" method="handleMessage" queue-names="traceQueue"/>
    </rabbit:listener-container>
    <!--走消息队列-->
</beans>